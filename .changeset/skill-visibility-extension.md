---
"@chrisptang/pi-skill-visibility": minor
---

Add Pi Skill Visibility, which hides explicitly configured skill names from Pi's automatic skill catalog and TUI completion without blocking manual use. It also adds local Pi and Claude Code skill-use analysis through `/skills-analysis`, and migrates the legacy `excluded-skills.json` setting to `pi-skill-visibility.json`.
