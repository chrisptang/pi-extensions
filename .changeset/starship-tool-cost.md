---
"@chrisptang/pi-starship": minor
---

Add a `$tools` variable to the `cost` module and show it by default as `(tools $x)` when tool results, such as subagent runs, reported cost. The session cost already includes that share.
